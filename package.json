{
  "name": "resume-compiler",
  "version": "1.0.0",
  "private": true,
  "description": "Compile resume.yaml into a styled HTML page and Letter-sized PDF",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "test": "npm run build && node scripts/verify.mjs",
    "preview": "npx -y serve dist"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.2.0",
    "js-yaml": "^4.1.0",
    "playwright": "^1.50.0"
  },
  "devDependencies": {
    "browser-sync": "^3.0.0",
    "chokidar": "^4.0.0"
  }
}
